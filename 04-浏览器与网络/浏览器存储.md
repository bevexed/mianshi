# 浏览器存储方案对比

> 原 `cookie和storage.md` 已并入本篇。
> 认证场景下 token 存哪里，见 [认证方案对比.md](认证方案对比.md)

## 全景对比

| | 容量 | 生命周期 | 随请求发送 | 同步/异步 | 跨域 |
|---|---|---|---|---|---|
| **Cookie** | ~4KB | 可设过期时间，默认关闭浏览器失效 | ✅ **每次都带** | 同步 | 受域名/路径限制 |
| **localStorage** | ~5-10MB | 永久，除非手动清 | ❌ | **同步（阻塞主线程）** | 同源隔离 |
| **sessionStorage** | ~5-10MB | **标签页关闭即失效** | ❌ | 同步 | 同源 + 同标签页 |
| **IndexedDB** | 大（通常几百 MB 起） | 永久 | ❌ | **异步** | 同源 |
| **Cache Storage** | 大 | 永久 | ❌ | 异步 | 同源 |

---

## 怎么选

- **少量配置、需要同步读** → localStorage
- **只在当前标签页有效**（如多开互不干扰的表单草稿）→ sessionStorage
- **结构化大数据、离线数据、需要索引查询** → IndexedDB
- **服务端也要读** → Cookie
- **静态资源离线** → Cache Storage + Service Worker

### 一个容易被忽略的性能点

`localStorage` 是**同步 API**，存取大对象（尤其配合 `JSON.parse`）
**会阻塞主线程**，直接影响 INP 指标。
频繁读写的大数据应该用 IndexedDB。

> 关联：[08-性能优化/INP与运行时性能.md](../08-性能优化/INP与运行时性能.md)

---

## Cookie 的关键属性

```
Set-Cookie: token=abc;
  Domain=.example.com;   // 作用域名（含子域）
  Path=/;
  Expires=...  / Max-Age=3600;
  HttpOnly;              // JS 读不到，防 XSS 窃取
  Secure;                // 只在 HTTPS 下发送
  SameSite=Lax           // 防 CSRF
```

**SameSite 三个值**：
- `Strict`——完全不跨站发送，体验差（从外链点进来是未登录态）
- `Lax`（现代浏览器默认）——顶层导航的 GET 请求会带，
  其余跨站请求不带
- `None`——跨站也带，但**必须同时设 `Secure`**

---

## IndexedDB

- 适合：离线应用、大量结构化数据、需要按索引查询
- 缺点：**API 很难用**（基于事件的事务模型）→
  实践中用 `idb` / `dexie` 这类封装库

```js
// 用 idb 封装后
const db = await openDB('mydb', 1, {
  upgrade(db) { db.createObjectStore('users', { keyPath: 'id' }) }
})
await db.put('users', { id: 1, name: 'a' })
```

【需确认】你项目里用过吗？没用过就诚实说了解但没实践，
别硬编——这题深挖起来细节很多（事务、版本升级、游标）。
