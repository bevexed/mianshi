# `<img>` 的 `alt` 和 `title`

- `alt` 是图片的文本替代：图片加载失败时可显示，也会被辅助技术读取，
  并帮助搜索引擎理解图片内容。
- 装饰性图片应写 `alt=""`，让屏幕阅读器忽略；有信息的图片写等价内容，
  不要堆关键词，也不要重复旁边已经存在的文字。
- `title` 只是补充提示，部分鼠标环境可能显示 tooltip；键盘、触屏和辅助技术支持不稳定，
  不能用它替代 `alt`、可见标签或正式 tooltip 组件。
